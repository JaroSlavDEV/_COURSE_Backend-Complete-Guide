1. Используем многострочный ввод. Каждый раз, когда строка заканчивается на открывающие `(` или `{` или `[` mongo shell переходит в многострочный ввод.
```
db.users.insert(
    [
        { name: 'John' },
        { name: 'Andrey', age: 35 }
    ]
)
```